### Intel.AES-NI.CBC: Implementing AES-256 encryption in CBC mode using Intel's AES-NI intrinsics



AES-NI instructions run at hardware speed, often several times faster than a pure-software AES implementation.

